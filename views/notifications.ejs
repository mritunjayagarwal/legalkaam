<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notifications</title>
</head>
<body>
        <% notifications.forEach((notification) => { %>
            <div>
                <p><%= notification %></p>
                <form action="/mark/read/" method="post">
                    <button type="submit" name="id" value="<%= notification._id %>" class="btn-link">Mark Read</button>
                </form>
            </div>
        <% }) %>
</body>
</html>